# Types package

Contains all the types shared by the server and the web app

## Commands

### Update the types package
```
pnpm --filter @repo/types build
```

Just want to trigger an update on server to update environment variables
