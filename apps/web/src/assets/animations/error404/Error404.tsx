import { AnimationProps, LottieAnimation } from "../core";

export const Error404 = (props: AnimationProps) => (
    <LottieAnimation
        {...props}
        animationName="error404.lottie"
    />
);
